apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: front-end
spec:
  containers:
  - name: nginx
    image: nginx # Per default it's from dockerhub
    resources:
      limits:
        memory: 128Mi
        cpu: "1"
  # - name: busybox
  #   image: busybox
